version: 2
jobs:
  build:
    docker:
      - image: circleci/python:3.7.4
    steps:
      - checkout
      - run: pip install -r requirements/dev.txt
workflows:
  version: 2
  build_circleci:
    jobs:
      - build
